# ── SO ──────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Ambiente / segredos (NUNCA versionar) ───────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.p8
*.pfx

# ── Trabalho local ──────────────────────────────────────────────────
tmp/
*.zip
CLAUDE.local.md
.claude/settings.local.json

# ── Saída do próprio AUDITOR quando rodado neste repo ───────────────
# Ver ADR pendente em docs/decisoes.md (§Pendentes): ainda não está decidido
# se `.auditor/` é versionado. Enquanto não decidir, fica fora do git.
.auditor/

# ── Python (caso o harness venha em Python) ─────────────────────────
__pycache__/
*.pyc
.venv/

# ── Node (caso o harness venha em Node) ─────────────────────────────
node_modules/
