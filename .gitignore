# ── SO ──────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Ambiente / segredos (NUNCA versionar) ───────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.p8
*.pfx

# ── Trabalho local ──────────────────────────────────────────────────
tmp/
*.zip
CLAUDE.local.md
.claude/settings.local.json

# ── `.auditor/` é VERSIONADO (ADR-010) — não ignorar ────────────────
# A saída do AUDITOR entra no git de propósito: é o que faz o checkpoint
# sobreviver a outra máquina e a CI. Consequência: relatório é artefato
# PUBLICADO — a redação de segredos (T-01) é pré-requisito, não opcional.

# ── Python (caso o harness venha em Python) ─────────────────────────
__pycache__/
*.pyc
.venv/

# ── Node (caso o harness venha em Node) ─────────────────────────────
node_modules/
